# Core scientific stack — compatible RANGES rather than exact pins, because
# Colab preinstalls a mutually consistent numpy/torch/scipy set and replacing
# one of them on disk leaves the kernel holding the old version in memory
# (the classic "numpy.dtype size changed" death). Ranges let pip keep what
# Colab ships whenever it satisfies them.
numpy>=1.26,<3
pandas>=2.1,<3
matplotlib>=3.8,<4
scikit-learn>=1.3,<2
torch>=2.1

# Geospatial
shapely>=2.0,<3
geopandas>=0.14
pyproj>=3.6
rasterio>=1.3
pystac-client>=0.7
earthengine-api>=0.1.380

# IO / utilities
pyarrow>=14          # parquet for the time-series cache
joblib>=1.3
tqdm>=4.66

# Development
pytest>=7.4
nbformat>=5.9        # notebooks are generated from scripts/
